import { PanoCoordinatesDirective } from './pano-coordinates.directive';

describe('PanoCoordinatesDirective', () => {
  it('should create an instance', () => {
    const directive = new PanoCoordinatesDirective();
    expect(directive).toBeTruthy();
  });
});
